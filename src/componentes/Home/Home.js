import React from 'react';
import Feed from '../Feed/Feed';

const Home = () => {
  return (
    <section className="conatiner mainContainer">
      <Feed />
    </section>
  );
};
export default Home;
